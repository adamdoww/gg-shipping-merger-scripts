# Copy this file to secrets.toml (locally) or paste its contents into
# Streamlit Cloud → app → Settings → Secrets.
#
# Setting a password enables the access gate. Leave it out / blank for open access.
app_password = "change-me-to-something-only-the-client-knows"
